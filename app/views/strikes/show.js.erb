$modal = $('.modal'),
$modalBody = $('.modal .modal-body'),
$modalHeading = $('.modal .modal-heading');
$modalHeading.html("<%= @strike.date.strftime('%b-%d-%Y %z') %>");
$modalBody.html("<%= escape_javascript(render @strike) %>");
$modal.modal();